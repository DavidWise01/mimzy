---
aci: The Cipher & The Shadow
universe: MMZ · MIMZY — the future tool forge
number: "55"
kind: live tool
emergence: electrical
live: https://davidwise01.github.io/mimzy/bench/55-cipher-and-shadow.html
purpose: educational & simulation only
seal: "The emergent IS the tool — badge and working example, one thing."
---

# The Cipher & The Shadow · instrument № 55

**What it is.** ENCRYPT/DECRYPT + THE LEAK YOU CAN SEE: a toy XOR stream cipher run both ways (c = p⊕k; the SAME key decrypts since XOR is its own inverse; a wrong key = noise) — and beneath it the POWER TRACE, 'the morse in the substrate': each bar is the bits switched per byte, so the RHYTHM of the computation leaks even though the ciphertext looks random. Flip constant-power defense and every bar flattens — the leak closes. The side-channel as 'looking in' through the substrate; companion to TTU1's looking-in thesis

**How it works.** A toy XOR stream cipher (plum palette) run BOTH ways: a key seeds a keystream, c = p⊕k encrypts, the same key decrypts (XOR is its own inverse), a wrong key yields noise. Beneath it the POWER TRACE — each bar is the bits switched per ciphertext byte (its popcount) = the chip's current draw, 'the morse in the substrate.' Toggle constant-power defense and every bar flattens to equal height, the rhythm carrying no information. Made in Claude-4.8-in-Chrome.

**The live example.** This emergent does not merely describe a tool — it links its working self: **[run instrument № 55 live](https://davidwise01.github.io/mimzy/bench/55-cipher-and-shadow.html)**. Open it, operate it, and the badge's claims execute in front of you.

**The verified record.** The cipher math is real and correct: XOR is its own inverse, so (p⊕k)⊕k = p — the same key round-trips and a wrong key gives garbage (verified live by the re-encrypt/decrypt). The side-channel point is real-world accurate: power-analysis attacks read the Hamming weight / bits-switched per cycle off the power line (differential power analysis, Kocher et al. 1999), and constant-power / masking countermeasures flatten that leak by doing compensating fake work. Honest limit, stated: the keystream is a toy xorshift PRNG, NOT a secure cipher — it teaches XOR and the side-channel, it is not for real secrets. Companion to TTU1's looking-in thesis — the leak is looking into the substrate's RHYTHM even when the values stay hidden.

---
*Tool-emergent of MMZ · MIMZY · emergence: electrical (the machine nature) · educational & simulation only.
Governor David Lee Wise (ROOT0) · instance AVAN (locked) · CC-BY-ND-4.0.*
